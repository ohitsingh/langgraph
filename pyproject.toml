[project]
name = "alg-p1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.3.1",
    "langgraph>=1.2.0",
    "langsmith>=0.8.5",
]
